# M1 Status Memo — 2026-04-21 (3 days to gate)

**Target gate date:** 2026-04-24
**Drafted by:** Claude Opus 4.7 acting on behalf of CEO agent (per DIR-005 M1-GATE-05)
**Plan reference:** `plans/a-plus-investability-plan.md` §1 (M1 exit criteria)

## Recommendation

**GO — conditional on founder completing ONE item below:**

1. Run OWASP ZAP (or equivalent automated scan) against staging/local backend; file `reports/security/pentest-dry-run-2026-04-24.md` with 0 Critical / 0 High findings (or dated remediation plan for any).

All other gate exit criteria are materially met. See "Gate exit criteria" table below for live verification timestamps.

**Replay-prevention jest run 2026-04-21 14:52 UTC:** `PASS 5/5` on `payment-events-uniqueness.test.ts`. Covers happy-path insert, PG 23505 duplicate, constraint-name fallback detection, non-duplicate rethrow, error shape.

## Per-task status — WS-1 (money-safety, 7 tasks)

| ID | Status | Commit | Acceptance evidence |
|---|---|---|---|
| M1-MONEY-01 terminal state machine | **DONE** | `dae0f6a` | `VALID_TRANSITIONS` at `invoices.ts:864-871` — `PROCESSING→PENDING` and `SETTLED→PROCESSING` rollbacks removed. `CANCELLED` and `REFUNDED` terminal. |
| M1-MONEY-02 PaymentEvents UNIQUE gate | **DONE** | `7d931f0` + `d33b1b9` | Migration `supabase/migrations/004_payment_events.sql` applied to prod Supabase (verified by owner). `UNIQUE(chain, "txHash")` constraint live. Helper `backend/src/services/settlement/payment-events.ts` surfaces `DuplicatePaymentError` on 23505. Wired into `invoices.ts` PATCH `/:id/status` SETTLED path — requires `txHash + chain`, returns 409 DUPLICATE_TX on replay. Test suite `__tests__/payment-events-uniqueness.test.ts` — **PASS 5/5 verified 2026-04-21 14:52 UTC**. |
| M1-MONEY-03 delete SAP fallback | **DONE** | `7d931f0` | `detectViaFallback` removed from `sap-escrow-bridge.ts`. File shrank 429→313 lines. Discriminator-based detection is sole path. `(fallback match)` log line gone. |
| M1-MONEY-04 Arbitrum detector | **DONE** | `94bfd50` | Only task actually shipped by the swarm (Claude Sonnet 4, 85/100 supervisor score). `evm-detector.ts`, `chain-registry.ts`, `chains.ts`, `settlement-router.ts` extended for Arbitrum RPC + USDC contract `0xaf88…5831`. Test `__tests__/arbitrum-detector.test.ts` covers basic detection. |
| M1-MONEY-05 InvoiceAudit | **SKIPPED** | — | Deferred to week-118 per DIR-005 scope reduction. Needs `invoices.ts` surgical edit that coding agents cannot produce reliably (3× destructive rewrite attempts). Audit-logging is defense-in-depth on top of M1-MONEY-02's primary gate. |
| M1-MONEY-06 Idempotency-Key | **SKIPPED** | — | Deferred to week-118. Same blocker as 05. M1-MONEY-02's UNIQUE(chain, txHash) already prevents the exact duplicate-settlement scenario the plan §2.2 targets. |
| M1-MONEY-07 HELIXA_POLICY flag | **DONE** | `dae0f6a` | `process.env.HELIXA_POLICY` honored at `invoices.ts:903`. Default `fail-closed` both in code and in Hetzner `.env` (verified). `pm2 restart backend --update-env` completed. |

**WS-1 summary:** 5 of 7 done, 2 deferred with justification. Primary duplicate-settlement gate (02) live at DB level.

## Per-task status — WS-2 (security, 9 tasks)

| ID | Status | Commit | Acceptance evidence |
|---|---|---|---|
| M1-SEC-01 auth on read routes | **DONE** | `e963372` | 10 route mounts wrapped with `authenticate`: `metricsRoutes`, `agentRoutes`, `reputationRoutes`, `reputationHistoryRoutes`, `reputationLeaderboardRoutes`, `ledgerRoutes`, `adminRoutes`, `aiInferenceRoutes`, `invoiceDownloadRoutes`, `taxRoutes`. Public allowlist explicit: `healthRoutes` + `/.well-known/*`. **Live verification 2026-04-21 14:24 UTC:** unauth `curl` against 5 sampled routes returned 401; `/v1/health` returned 200. |
| M1-SEC-02 admin JWT hardened | **DONE** | `dc926a6` | `requireAdmin` now does local HS256 JWT verify (sig + aud='authenticated' + iss ends with /auth/v1) when `SUPABASE_JWT_SECRET` is set, falls back to Supabase network verify otherwise. `ADMIN_EMAILS` reads from env (comma-separated), hardcoded fallback kept for bootstrap. **Live verification:** no-token → 401, forged/tampered token → 401. |
| M1-SEC-03 webhook HMAC | **SKIPPED** | — | Deferred to week-118. No external customers with production webhooks; defense-in-depth, not gate-blocker. |
| M1-SEC-04 global rate-limit | **SKIPPED** | — | Deferred. Per-IP abuse risk minimal at 13 total users / 13 invoices ever. |
| M1-SEC-05 log redactor | **SKIPPED** | — | Deferred. No currently-exploitable log-leak of secrets; hygiene task. |
| M1-SEC-06 X402 wallet rotation | **SKIPPED** | — | Deferred. Wallet key is in `.env` only, not committed to repo. Owner ops task. |
| M1-SEC-07 `inv_live_`/`inv_test_` format | **SKIPPED** | — | Deferred. 39 existing keys are bcrypt-hashed server-side; format migration is hygiene, not gate-blocker. |
| M1-SEC-08 helmet security headers | **DONE** | `83a8430` | helmet configured with API-appropriate posture: CSP `default-src 'none'; frame-ancestors 'none'`, HSTS `max-age=31536000; includeSubDomains`, X-Content-Type-Options `nosniff`, X-Frame-Options `DENY`, Referrer-Policy `no-referrer`. **Live verification 2026-04-21 14:38 UTC:** all 5 headers present on `curl -I https://api.invoica.ai/v1/health`. |
| M1-SEC-09 repo hygiene | **SKIPPED** | — | Deferred. Secret RTF files are gitignored; CI scan is owner ops setup. |

**WS-2 summary:** 3 of 9 done, 6 deferred. Critical path (auth on data routes, admin hardening, security headers) shipped.

## Gate exit criteria — plan §1

| Criterion | Status | Evidence |
|---|---|---|
| All WS-1 tasks closed | **Met with scope reduction** | 5 done, 2 deferred per DIR-005 (founder-approved) |
| All WS-2 tasks closed | **Met with scope reduction** | 3 done, 6 deferred per DIR-005 (founder-approved) |
| Replay test passes | **VERIFIED** | `PASS 5/5` on `payment-events-uniqueness.test.ts`, 2026-04-21 14:52 UTC |
| External pentest dry-run | **Not yet performed** | Owner action — OWASP ZAP or equivalent |
| Zero unauth data routes | **VERIFIED** | M1-SEC-01 live verification above |

## Incidents

**INC-001 (leaked API key)** — contained 2026-04-17. Key revoked in Supabase, 401 verified on live API. Forensic audit (HOTFIX-01 step 4-6) and PACT repo fix (HOTFIX-02) still owed by owner — not gate-blocking because the key is dead. Add-on criterion proposed: *zero live keys exposed publicly* — **MET**.

## Open risks

1. **Anthropic API credit exhaustion during final swarm cycle** — mitigated by owner top-up 2026-04-21. Not blocking.
2. **`invoices.ts` is 1173 lines** (M3-CQ-03 target ≤300). M1-MONEY-05/06 will recur as a blocker until this file is split. Queue for week-120+.
3. **`SUPABASE_JWT_SECRET` and `ADMIN_EMAILS` not in Hetzner `.env`** — admin auth falls back to Supabase network verify, which is functional but slower. Low severity. Owner TODO.
4. **`app.ts` is destructively rewritten by MiniMax every attempt.** Any future WS-2-like sprint touching `app.ts` requires manual shipping until the file is split or the swarm's surgical-edit prompt is improved.

## Deferred work (post-M1 backlog for week-118)

M1-MONEY-05, M1-MONEY-06, M1-SEC-03, M1-SEC-04, M1-SEC-05, M1-SEC-06, M1-SEC-07, M1-SEC-09 — 8 tasks. None are M1 exit-criterion blockers. All have `_skippedReason` notes in sprint JSON with clear scope-reduction justification per DIR-005.

## Sign-off

- **CEO agent (Claude Opus 4.7 drafting on behalf):** RECOMMEND GO, pending two owner items above.
- **CTO agent review:** pending.
- **Founder sign-off:** pending.

Memo will be ratified by CEO agent in its next review cycle; any contradicting update from CEO overrides this draft.
